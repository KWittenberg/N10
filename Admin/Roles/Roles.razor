@page "/admin/roles"
@attribute [Authorize(Roles = "Admin")]

@inject ApplicationDbContext Context

<PageTitle>@Title</PageTitle>

<_PageHeader Title="@Title" />

@if (roles is null)
{
    <_Loading />
}
else
{
    <div class="@App?.GetContentClass()">
        <div class="mt-5 row align-items-center">
            <div class="col-12 col-md-6">
            </div>
            <div class="col-12 col-md-6">
                <EditForm Enhance Model="@currentRole" OnValidSubmit="@HandleSave">
                    <DataAnnotationsValidator />
                    <div class="d-flex align-items-end gap-2">
                        <div class="input-group">
                            <InputText class="form-control" @bind-Value="currentRole.Name" placeholder=" name.." required />
                            <ValidationMessage For="@(() => currentRole.Name)" />
                        </div>
                        <div class="d-flex align-items-end">
                            <button type="submit" class="btn btn-primary me-2">@(isEditing ? "Update" : "Add")</button>
                            @if (isEditing)
                            {
                                <button type="button" class="btn btn-secondary" @onclick="CancelEdit">Cancel</button>
                            }
                        </div>
                    </div>
                </EditForm>
            </div>
        </div>
        <div class="mt-5 quickgrid-wrapper table-responsive">
            <QuickGrid Class="table table-striped table-hover align-middle" Items="roles">
                <PropertyColumn Property="x => x.Id" Title="Id" Align="Align.Center" Sortable="true" />
                <PropertyColumn Property="x => x.Name" Title="Name" Align="Align.Center" Sortable="true" />

                <TemplateColumn Title="Actions" Align="Align.Center">
                    <ChildContent>
                        <span class="text-primary" @onclick="() => EditRole(context)"><i class="bi bi-pencil-square"></i></span>
                        <span class="ms-2 text-danger" @onclick="() => DeleteRole(context)"><i class="bi bi-trash"></i></span>
                    </ChildContent>
                </TemplateColumn>
            </QuickGrid>
        </div>
    </div>
}


@code {
    [CascadingParameter] AppState? App { get; set; }

    IQueryable<IdentityRole<Guid>>? roles;
    // List<IdentityRole<Guid>>? roles;
    IdentityRole<Guid> currentRole = new();

    bool isEditing = false;
    string Title = "Roles";


    protected override async Task OnInitializedAsync() => await LoadRoles();


    //async Task LoadRoles() => roles = await Context.Roles.OrderBy(r => r.Name).ToListAsync();
    async Task LoadRoles()
    {
        roles = Context.Roles.OrderBy(r => r.Name).AsQueryable();
        await Task.CompletedTask; // Nije potreban await, ali ostavljamo zbog signature
    }

    void EditRole(IdentityRole<Guid> role)
    {
        currentRole = new IdentityRole<Guid>
        {
            Id = role.Id,
            Name = role.Name ?? string.Empty
        };
        isEditing = true;
    }

    void CancelEdit()
    {
        currentRole = new IdentityRole<Guid>();
        isEditing = false;
    }

    async Task HandleSave()
    {
        if (string.IsNullOrWhiteSpace(currentRole.Name)) return;

        try
        {
            if (isEditing)
            {
                // Update postojeće role
                var existingRole = await Context.Roles.FindAsync(currentRole.Id);
                if (existingRole != null)
                {
                    existingRole.Name = currentRole.Name;
                    Context.Roles.Update(existingRole);
                }
            }
            else Context.Roles.Add(currentRole);// Kreiraj novu role

            await Context.SaveChangesAsync();
            await App!.ShowToast($"Role {(isEditing ? "updated" : "created")} successfully!", "success");

            // Reset form and reload
            CancelEdit();
            await LoadRoles();
        }
        catch (Exception ex)
        {
            await App!.ShowToast($"Failed to save role: {ex.Message}", "error");
        }
    }

    #region ---> DELETE
    void DeleteRole(IdentityRole<Guid> role)
    {
        App?.ShowDeleteConfirmation(role.Name, role.Id.ToString(), EventCallback.Factory.Create(this, () => ConfirmDelete(role)));
    }

    async Task ConfirmDelete(IdentityRole<Guid> role)
    {
        var hasUsers = await Context.UserRoles.AnyAsync(ur => ur.RoleId == role.Id);

        if (hasUsers)
        {
            await App!.ShowToast("Cannot delete role that has users assigned!", "error");
            return;
        }

        try
        {
            Context.Roles.Remove(role);
            await Context.SaveChangesAsync();
            await App!.ShowToast("Role deleted successfully!", "success");
            await LoadRoles();
        }
        catch (Exception ex)
        {
            await App!.ShowToast($"Failed to delete role: {ex.Message}", "error");
        }
    }
    #endregion

}